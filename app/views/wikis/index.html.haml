.container

  %h1 Wikis

  .row
    .columns.eight
      -@wikis.each do |wiki|
        %h4
          = link_to wiki.subject, wiki
          %small= link_to "Edit", edit_wiki_path(wiki)
      
    .columns.two
      -if policy(Wiki.new).create?
        = link_to "New Wiki", new_wiki_path, class: 'button'